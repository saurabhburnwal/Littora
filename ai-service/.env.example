PORT=8000
# Directory containing the deployed custom .pt model artifacts. The repository
# intentionally does not contain model weights; mount or copy them here in the
# deployment environment.
MODEL_DIR=./models
